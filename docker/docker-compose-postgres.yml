version: '3.8'

services:
  postgres_funding:
    image: postgres
    container_name: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: nm[pv3[0%&#{!td745@#
      POSTGRES_DB: funding_prod
    volumes:
      - /var/data/db_main:/var/lib/postgresql/data
